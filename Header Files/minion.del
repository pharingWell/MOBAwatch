import "creature.del";
import "../error.ostw";

define MINION_WAVE_SIZE: [3,1,3,0,0];
define EMinionType_toArray: [EMinionType.MELEE,EMinionType.MAGE,EMinionType.RANGED,EMinionType.SEIGE,EMinionType.BRUTE];
globalvar Minion[] minions;

globalvar Number[] waves; //points to the index of the first minion in the wave. When removing a mi
//zeroes?

define TEAM1_MINION_SPAWN: Vector(0,0,10);

define TEAM2_MINION_SPAWN: Vector(0,0,-10);

enum EMinionType{
    MELEE,
    MAGE,
    RANGED,
    SEIGE,
    BRUTE
}
class Minion : Creature
{
    public EMinionType type;
    public constructor(in Team team, in EMinionType type){
        entity_constructor();
        switch (team){
            case Team.Team1:
                color = Color.Team1;
                position = TEAM1_MINION_SPAWN;
                velocity = Vector(0,0,-1);
                break;
            case Team.Team2:
                color = Color.Team2;
                position = TEAM2_MINION_SPAWN;
                velocity = Vector(0,0,1);
                break;
            case Team.All:
                async throwError({
                    message: "Tried to create a neutral minion.",
                    source: "Minion, constructor, case Team.ALL",
                    severity: EErrorSeverity.FATAL
                    });
                break;
        }
        LogToInspector(this.subclass);
        appearance = ["ι","ί",team == Team.Team1 ? "R" : "Я","ϊ","ΐ"][type];
        if(!appearance){
            throwError({
                message: "Minion rendered with no appearance", 
                source: "Minion, constructor, apperance",
                severity: EErrorSeverity.WARNING
                });
        }
        
        Wait(0.016);
        visibility = true;
    }

    public void destroy(){
        //TODO: remove from tickOffsetSorted
    }

}