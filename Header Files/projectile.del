import "entity.del";
import "structures.del";

struct ProjectileSource{
    Boolean isStructure;
    CreaturePlayer creaturePlayerSource;
    Structure structureSource;
}

class Projectile : Entity{
    
    public EEntitySubclass subclass: EEntitySubclass.PROJECTILE;
    Any render; //stores the effect id of the projectile (or projectile effect id)
}