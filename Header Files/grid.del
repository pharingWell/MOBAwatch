import "../Header Files/entity.del";
import "../error.ostw";
playervar Vector[] debug1;
class Cell{
    public Entity[] dynamic;
    public Effect[] statics;
}
define NULL_CELL: <Cell>0;
struct GridStruct{ //only used once
    public Cell[] cells;
}
globalvar GridStruct grid;

define GRID_BASE: Vector(-GRID_COLUMNS/2 * CELL_WIDTH,0,-GRID_ROWS/2 * CELL_HEIGHT);

define CELL_WIDTH: 10;
define CELL_HEIGHT: 10;

# X Count
define GRID_COLUMNS: 7;
# Y Count
define GRID_ROWS: 4;


